[package]
name = "rambots"
version = "0.1.0"
edition = "2021"

[lib]
name = "rambots"
path = "src/lib.rs"

[dependencies]
vexide = { workspace = true }
evian = { workspace = true }
log = { workspace = true }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
